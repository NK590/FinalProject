<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="memberMapper">

	<select id="checkEmail" parameterType="String" resultType="com.helper.member.MemberDTO">
		select * from tb_member
			where mem_id = #{mem_id}
	</select>
	
	<insert id="insert" parameterType="com.helper.member.MemberDTO">
		insert into tb_member values(mem_seq.nextval, #{mem_id}, #{mem_pw}, #{mem_nick}, #{mem_std_key}, 'N', 0)
	</insert>
	
	<select id="loginOk" parameterType="String" resultType="com.helper.member.MemberDTO">
		select * from tb_member
			where where mem_id = #{mem_id} and mem_pw = #{mem_pw}"
	</select>
	
	
	


</mapper>